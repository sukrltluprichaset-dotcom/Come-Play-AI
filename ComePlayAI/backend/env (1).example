# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริงของคุณ (ห้าม commit .env ตัวจริงขึ้น git)

APP_PORT=8080

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=ใส่รหัสผ่านที่ตั้งไว้ตอนติดตั้ง PostgreSQL ตรงนี้
DB_NAME=comeplayai
DB_SSLMODE=disable
